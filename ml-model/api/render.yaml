services:
  - type: web
    name: lasis-ml
    runtime: python
    rootDir: ml-model/api
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0